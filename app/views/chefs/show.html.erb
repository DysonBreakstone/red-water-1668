<h1><%= @chef.name %></h1>
<%= link_to "#{@chef.name}'s ingredients", chef_ingredients_path(@chef) %>
<%= "#{@chef.name}'s most popular ingredients" %>
<ul>
  <% @pop_ingredients.each do |ingredient| %>
    <%= ingredient.name %>
  <% end %>
</ul>